$(document).ready(function(){
    //var userComment = document.getElementById('addComment');
   //function submitComment() {});

    //Set the carousel options
    $('#quote-carousel').carousel({
        pause: true,
        interval: 4000
    });

});